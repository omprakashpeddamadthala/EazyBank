spring:
  application:
    name: accounts
  profiles:
    active: qa
  datasource:
    url: jdbc:postgresql://ep-winter-star-ad8q0gjh-pooler.c-2.us-east-1.aws.neon.tech/accountsdb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_IyHQEVcL2W4r
  jpa:
    show-sql: true
  sql:
   init:
    mode: always
  config:
    import: optional:configserver:http://localhost:8071
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

springdoc:
  api-docs:
    version: openapi_3_0
server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    readiness-state:
      enabled: true
    liveness-state:
      enabled: true
  endpoint:
    shutdown:
      access: unrestricted
    health:
      probes:
        enabled: true
  info:
    env:
      enabled: true

eureka:
  instance:
    preferIpAddress: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/

info:
  app:
    name: "accounts"
    description: "Eazy Bank Accounts Application"
    version: "1.0.0"

logging:
  level:
    com:
      bytes:
        accounts: DEBUG