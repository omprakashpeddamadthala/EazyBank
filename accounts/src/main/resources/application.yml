spring:
  application:
    name: accounts
  profiles:
    active: qa
  datasource:
    url: jdbc:mysql://localhost:3306/accountsdb
    username: root
    password: root
  jpa:
    show-sql: true
  sql:
   init:
    mode: always
  config:
    import: optional:configserver:http://localhost:8071
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

springdoc:
  api-docs:
    version: openapi_3_0
server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"